## 取り組んだ課題一覧
- mosya React 入門編 5 (React Hooks)
- mosya React 中級編 3-1, 3-2 (ReactでAPIに接続しよう)

## わかったこと
### React Hooksで学んだこと
- **useEffect**: 副作用を扱うHook、コンポーネントのレンダリング後に実行される
- **useRef**: 描画に影響しない値の保持、DOMへの直接アクセス
- **useRefでDOM操作**: フォーカス制御やスクロール位置の操作など
- **useEffectの依存配列**: 特定の値の変更時のみ処理を実行する仕組み
- **カスタムフック**: ロジックを再利用可能な形で切り出す方法
- **useReducer**: 複雑な状態管理、複数の状態を一元管理

### React中級編 API接続で学んだこと
- **fetchを使ったAPIからのデータ取得**: 外部APIとの通信方法
- **SWRライブラリの活用**: データフェッチングの効率化とキャッシュ管理

## 次やること
- mosya React中級編の残りのセクション
- 実際のAPIを使ったアプリケーション開発
- エラーハンドリングやローディング状態の実装

## 感じたこと
React HooksとAPI接続の学習により、実践的なWebアプリケーション開発のスキルが身についた。特にuseEffectとfetchを組み合わせたデータ取得は、多くのアプリで必須となる技術。SWRライブラリを使うことで、データフェッチングがより簡潔に書けることに驚いた。キャッシュ機能やリトライ機能が組み込まれているため、本番環境でも使える実用的な知識を得られた。APIとの連携ができるようになったことで、作れるアプリケーションの幅が大きく広がった。

## 学習時間
Today：2h
Total：64h